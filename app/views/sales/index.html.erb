<h1>Transactions</h1>

<table>
  <tr>
    <th>Customer</th>
    
    <th>Employee</th>
    <th>Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sales.each do |sale| %>
  <tr>
    <td><%= sale.customer.name %></td>    
    <td><%= sale.employee_id %></td>
    <td><%= sale.date %></td>
    <td><%= link_to 'Show', sale %></td>
    <td><%= link_to 'Edit', edit_sale_path(sale) %></td>
    <td><%= link_to 'Delete', sale, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Process New Transaction', new_sale_path %>
